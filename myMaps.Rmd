---
title: "My Maps"
output: pdf_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r Source, echo=FALSE, message=FALSE, warning=FALSE}
source("R/gps2trips.R")

library(targets)
tar_load(clusters_per_date)

for(date in clusters_per_date$date) {
  map_clusters(clusters_per_date, date)
}
```


